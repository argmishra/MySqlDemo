## Create Schema
```mysql
create database if not exists anuragDB;
```

## Use Schema
```mysql
use anuragDB;
```

## Show Schemaes
```mysql
show databases;
show databases like "%uragdb"; 
```

## Drop Schema
```mysql
create database if not exists anuragDB; 
```